# Local Development Override
# Docker Compose automatically merges this file with docker-compose.yml for local runs.
services:
  app:
    volumes:
      - ./src:/app/src
      - ./public:/app/public
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./storage:/app/storage
